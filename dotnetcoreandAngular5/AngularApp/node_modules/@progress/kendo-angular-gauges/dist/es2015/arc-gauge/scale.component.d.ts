import { ScaleComponent } from '../base-components';
import { ConfigurationService } from '../services';
import { ArcScale, RadialLabels } from '../types';
/**
 * The scale configuration options of the Arc Gauge.
 */
export declare class ArcScaleComponent extends ScaleComponent implements ArcScale {
    protected configurationService: ConfigurationService;
    labels?: RadialLabels;
    rangeDistance?: number;
    startAngle?: number;
    endAngle?: number;
    constructor(configurationService: ConfigurationService);
}
