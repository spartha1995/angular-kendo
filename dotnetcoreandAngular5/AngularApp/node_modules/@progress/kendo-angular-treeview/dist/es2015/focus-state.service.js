import { Injectable } from "@angular/core";
import { Subject } from "rxjs/Subject";
/**
 * @hidden
 */
export class FocusStateService {
    constructor() {
        this.changes = new Subject();
        this.activeContent = false;
        this.isActive = false;
    }
    hasActiveContent() {
        return !this.isActive && this.activeContent;
    }
    focus(index) {
        this.isActive = true;
        this.changes.next({ focused: true, index });
    }
    blur(index) {
        this.isActive = false;
        this.changes.next({ focused: false, index });
    }
    focusin(_) {
        this.activeContent = true;
    }
    focusout(_) {
        this.activeContent = false;
    }
}
FocusStateService.decorators = [
    { type: Injectable },
];
/** @nocollapse */
FocusStateService.ctorParameters = () => [];
