import { Margin } from './margin.interface';
/**
 * The padding configuration for each side
 * (see the [basic usage example]({% slug api_charts_titlecomponent %})).
 */
export interface Padding extends Margin {
}
